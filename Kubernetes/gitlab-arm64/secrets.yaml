apiVersion: v1
kind: Secret
metadata:
  name: gitlab-root-password
  namespace: gitlab
  labels:
    app.kubernetes.io/name: gitlab
    app.kubernetes.io/component: server
type: Opaque
# Value will be created via: kubectl create secret generic gitlab-root-password \
#   --from-file=gitlab_root_password=./secrets/gitlab_root_password.txt \
#   --namespace gitlab
# Placeholder below — replace or use the kubectl command above
data: {}
---
apiVersion: v1
kind: Secret
metadata:
  name: gitlab-tls
  namespace: gitlab
  labels:
    app.kubernetes.io/name: gitlab
    app.kubernetes.io/component: tls
type: kubernetes.io/tls
# Value will be created via: kubectl create secret tls gitlab-tls \
#   --cert=./ssl/gitlab.local.crt \
#   --key=./ssl/gitlab.local.key \
#   --namespace gitlab
# Placeholder below — replace or use the kubectl command above
data:
  tls.crt: ""
  tls.key: ""
