apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: gitlab

resources:
  - namespace.yaml
  - gitlab-pvc.yaml
  - gitlab-deployment.yaml
  - gitlab-service.yaml
  - ingress.yaml

# Runner is installed separately via Helm — see register-runner.sh
